/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', "firebasestorage.googleapis.com" ], // Add the domain here
      },
}

module.exports = nextConfig
